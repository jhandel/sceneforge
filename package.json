{
  "name": "@t3lnet/sceneforge",
  "version": "0.2.0",
  "description": "SceneForge monorepo for recording and playing demo videos from YAML definitions",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @t3lnet/sceneforge-extension dev",
    "build": "bun run build:shared && bun run build:playwright && bun run build:extension && bun run build:sceneforge",
    "build:shared": "echo 'Shared package uses TypeScript source directly'",
    "build:playwright": "echo 'Playwright package uses TypeScript source directly'",
    "build:extension": "bun run --filter @t3lnet/sceneforge-extension build",
    "build:sceneforge": "bun run --filter @t3lnet/sceneforge build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "bun run test:unit && bun run test:e2e",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "chrome": "open -a 'Google Chrome' 'chrome://extensions'"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "@types/chrome": "0.0.304",
    "@types/node": "^20.10.6",
    "eslint": "^8.57.0",
    "tsup": "^8.5.0",
    "typescript": "^5.7.3",
    "vitest": "^1.6.1"
  }
}
